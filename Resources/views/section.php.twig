<!-- {{ name }} Section -->
    <section id="{{ name }}">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2 class="section-heading">{{ name }} header</h2>
                    <h3 class="section-subheading text-muted">{{ name }} subheader</h3>
                </div>
            </div>
            {% for part in parts %}
                @{{ part }}@
            {% endfor %}
        </div>
    </section>
